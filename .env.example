# Required at runtime - Render injects this automatically
PORT=10000

# Set to production on Render
NODE_ENV=production

# Logging level (debug | info | warn | error)
LOG_LEVEL=info

# Required at BUILD TIME for Vite frontend (set in build command, not env vars panel)
# PORT=3000
# BASE_PATH=/
